{
  "functions": [
    {
      "source": "functions",
      "codebase": "default",
      "ignore": [
        ".git",
        "node_modules",
        "firebase-debug.log",
        "firebase-debug.*.log"
      ],
      "predeploy": [
        "npm --prefix \"$RESOURCE_DIR\" run lint",
        "npm --prefix \"$RESOURCE_DIR\" run build"
      ]
    }
  ]
}
